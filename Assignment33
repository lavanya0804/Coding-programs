#include<tdio.h>
int main(void)
{
 int arr[600],i,count=0;
 for(i=0;i<=4;i++)
	 scanf("%d",&arr[i]);
	 for(i=0;i<=4;i++)
	 {
	     if(arr[i]>=10)
	     count=count+1;
	 }
	 printf("%d",count);
	 	return 0;
}
